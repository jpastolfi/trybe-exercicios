/* Elabore alguns códigos e imprima o resultado no console usando o console.log, um para cada operação aritmética básica. Seu código deve ter duas constantes, a e b, definidas no começo com os valores que serão operados. Use o console.log() para exibir o que cada o exercício pede. */
let a = 15;
let b = 3;
/* Escreva códigos para:
Adição (a + b) */
let adicao = a + b;
console.log(adicao);

/* Subtração (a - b) */
let soma = a - b;
console.log(soma);

/* Multiplicação (a * b) */
let multiplicacao = a * b;
console.log(multiplicacao);

/* Divisão (a / b) */
let divisao = a / b;
console.log(divisao)

/* Módulo (a % b) */
let modulo = a % b;
console.log(modulo);